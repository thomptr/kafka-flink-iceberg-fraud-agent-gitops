apiVersion: v1
kind: Service
metadata:
  name: fraud-alert-agent
  namespace: fraud-agent
spec:
  selector:
    app: fraud-alert-agent
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
