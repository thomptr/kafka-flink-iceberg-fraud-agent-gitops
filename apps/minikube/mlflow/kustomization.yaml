apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/mlflow
patches:
  - target:
      kind: Service
      name: mlflow
      namespace: mlflow
    patch: |-
      - op: add
        path: /spec/type
        value: NodePort
