# Sample Flink Jobs

This overlay contains Minikube-targeted sample Flink jobs.

Expected dependencies:

- Strimzi and the `platform-cluster` Kafka resource are healthy
- The Flink Kubernetes Operator is reconciled
- Any application-specific secrets remain out of Git
